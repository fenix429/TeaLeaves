'use strict';

describe('Directive: revealModal', function () {
  beforeEach(module('TeaLeavesApp'));

  var element;

	/*
  it('should make hidden element visible', inject(function ($rootScope, $compile) {
    element = angular.element('<reveal-modal></reveal-modal>');
    element = $compile(element)($rootScope);
    expect(element.text()).toBe('this is the revealModal directive');
  }));
	*/
});
