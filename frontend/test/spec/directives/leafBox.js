'use strict';

describe('Directive: leafBox', function () {
  beforeEach(module('TeaLeavesApp'));

  var element;

	/*
  it('should make hidden element visible', inject(function ($rootScope, $compile) {
    element = angular.element('<leaf-box></leaf-box>');
    element = $compile(element)($rootScope);
    expect(element.text()).toBe('this is the leafBox directive');
  }));
	*/
});
